import { config } from "../config.js"; import { navigationItems } from "../app/navigation.js"; import { session } from "../app/session.js";
export function appHeader(){return `<header class="app-header"><div class="header-top"><a class="brand" href="#/"><span class="brand-mark">LI</span><span><strong>${config.appName}</strong><small>Arquitetura modular 4.0</small></span></a><div class="header-status"><span class="status-chip">XP <strong id="header-xp">${session.progress?.xp||0}</strong></span><span class="status-chip">${config.online.enabled?"Online":"Modo local"}</span></div></div><nav class="main-nav">${navigationItems.map(i=>`<a href="${i.href}" data-nav-path="${i.href.replace("#","")}">${i.label}</a>`).join("")}</nav></header>`}
addEventListener("lab:progress-changed",()=>{const e=document.querySelector("#header-xp");if(e)e.textContent=session.progress.xp});
